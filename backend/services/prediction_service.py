"""
Word & Phrase Prediction Service — Owner: M2 (F29)
Uses a local DistilGPT-2 model via HuggingFace transformers.

STATUS: Skeleton only. predict_words() and predict_phrase() are
added once NLP checks (F26-F28) are working, per Build Guide
build order (Section 3).
"""

# DistilGPT-2 pipeline will be loaded here, once, at module level.